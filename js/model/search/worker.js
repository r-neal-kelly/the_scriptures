import*as Utils from"../../utils.js";import*as Data from"../data.js";import*as Parser from"./parser.js";import*as Executor from"./executor.js";const executor=new Executor.Instance;onmessage=function(e){const{file_transfer:t,expression:r}=e.data;Object.setPrototypeOf(t,Data.File.Transfer.Instance.prototype);const s=executor.Node_Or_Help(t.Dictionary(),r);0,postMessage(executor.Execute_With_Node(t.Text(),s))};